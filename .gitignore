# in case stow creates .emacs.d as a symlink (which happens if there was none previously)
# and emacs starts putting its files into this directory
emacs/.emacs.d/*
!emacs/.emacs.d/init.el
!emacs/.emacs.d/emacsd/*
